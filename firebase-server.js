// firebase-server.js has been moved to the private/ directory to avoid exposing
// server-side credentials and service account keys. If you need to run the
// server locally, copy `private/firebase-server.js` into a secure location and
// follow the setup instructions in that file.

console.log('Note: firebase-server.js has been moved to private/firebase-server.js');
